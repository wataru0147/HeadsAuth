    <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Brands <span class="caret"></span></a>
              <ul class="dropdown-menu">
             
              <% Brand.all.published.each do |brand| %>
   <li> <%= link_to brand.name, products_path(brand: brand.name) %></li>
    <% end %> </ul>